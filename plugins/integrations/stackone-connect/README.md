# stackone-connect

Account linking via StackOne Connect Sessions and the Hub React component.

## Install

```bash
/plugin marketplace add stackonehq/agent-plugins
/plugin install stackone-connect@stackone-agent-plugins
```

## Use

- "Connect a provider"
- "Embed the integration picker"
- "Create a connect session"
- "Handle account webhooks"
